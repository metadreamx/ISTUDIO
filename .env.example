# App URL (provided by the platform)
APP_URL=

# Optional: used by the GitHub Pages PWA so iPhone/Safari can call Gemini through a relay.
VITE_GEMINI_RELAY_URL=

# Netlify builds set this through netlify.toml so the PWA uses the same-site Netlify Function relay.
VITE_HOSTING_TARGET=
